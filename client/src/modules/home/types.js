import Constants from './constants';
const NAME = Constants.NAME;

// Action types that this module dispatches
const ACTION_ONE = NAME + '/ACTION_ONE';
const ACTION_TWO = NAME + '/ACTION_TWO';

export default {
    ACTION_ONE,
    ACTION_TWO
};
